#keybindings for programs
super + Return
	$TERMINAL
super + t
	$TERMINAL
super + {_,shift} + b
	{$BROWSER,$BROWSER2}
super + o
    xournalpp

#generic
super + {_,shift} + a
	{$TERMINAL -e pulsemixer,sound.sh mute}
super + {_,shift} + p
	{sound.sh up 10,sound.sh dowm 10}

#menu keybindings
super + {_,shift} + x
	{fin.sh,xkill}
super + {_,shift} + r
    {dmenu_run -fn 'Hack-15',screen.sh}
super + g
	passmenu -i
